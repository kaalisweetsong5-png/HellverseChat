# Railway Deployment Configuration
[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "node backend/server.js --production"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[variables]
  NODE_ENV = "production"
  JWT_SECRET = "railway-secret-key-change-in-dashboard"